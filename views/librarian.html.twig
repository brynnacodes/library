<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>L.S.D.M.</title>
    </head>
    <body>
        <div class="container">
            <h3>Titles in the Library</h3>
            {% if books is not empty%}
            <ul>
                {% for book in books%}
                    <a href="/books/{{book.getId}}"><li>{{book.getTitle}}</li></a>
                {% endfor%}
            {% else %}
                <p>
                    There are no books in library.
                </p>
            {% endif %}

            <form action="/add_book" method ="post">
                <label for="title">Add book title:</label>
                <input type="text" name="title">

                <button class="btn" type="submit">Add book</button>
            </form>

            <form action="/add_author" method="post">
                <label for="name">Add author name:</label>
                <input type="text" name="name">

                <button class="btn" type="submit">Add author</button>
            </form>
            <a href="/"><button>Home</button></a>
        </div>
    </body>
</html>
